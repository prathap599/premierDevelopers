</section>
<section>
    <style>
        /* The Modal (background) */
.modal {
  display: {{valid}}; /* Hidden by default */
  position: fixed; /* Stay in place */
  z-index: 1; /* Sit on top */
  padding-top: 100px; /* Location of the box */
  left: 0;
  top: 0;
  width: 100%; /* Full width */
  height: 100%; /* Full height */
  overflow: auto; /* Enable scroll if needed */
  background-color: rgb(0,0,0); /* Fallback color */
  background-color: rgba(0,0,0,0.4); /* Black w/ opacity */
}

/* Modal Content */
.modal-content {
  background-color: #fefefe;
  margin: auto;
  padding: 20px;
  border: 1px solid #888;
  width: 80%;
}

/* The Close Button */
.close {
  color: #aaaaaa;
  float: right;
  font-size: 28px;
  font-weight: bold;
}

.close:hover,
.close:focus {
  color: #000;
  text-decoration: none;
  cursor: pointer;
}
    </style>
    <!-- The Modal -->
<div id="error" class="modal">

  <!-- Modal content -->
  <div class="modal-content">
    <span class="close">&times;</span>
    <p>Please fill the form correctly</p>
  </div>

</div>

    <div class="container">
        <div class="row signup-and-login">
            <div
                class="col-sm-12 col-md-5 col-lg-5 col-xl-5 flex direction-column-flex justify-center-flex align-items-center-flex">
                <img src="images/login.svg" alt="login">
                <form action="/auth" class="form-style flex flex-no-wrap justify-center-flex direction-column-flex"
                    method="post">
                    <input type="text" name="login" style="display: none;" value="true"/>
                    <input type="email" name="email" class="text-input-theme" placeholder="Email address" />
                    <input type="password" name="password" class="text-input-theme" placeholder="Password address">

                    <button type="submit" class="theme-btn">Login</button></form>
                <p>Not a member ? Sign up now</p>
            </div>
            <div class="col-sm-12 col-md-6 col-lg-6 col-xl-6">
                <img src="images/signup.svg" alt="login">
                <form action="/auth" class="form-style flex flex-no-wrap justify-center-flex direction-column-flex"
                    method="post">
                    <input type="email" name="email" class="text-input-theme" placeholder="Email address" />
                    <input type="password" name="password" class="text-input-theme" placeholder="Password address">
                    <input type="tel" name="tel" class="text-input-theme" placeholder="Phone">
                    <input type="number" name="investment" class="text-input-theme" placeholder="Investment upto">
                    <input type="text" name="comments" class="text-input-theme"
                        placeholder="What is your project is about">
                    <button type="submit" class="theme-btn">Register</button></form>

            </div>
        </div>
    </div>
    <script>
            // Get the modal
var modal = document.getElementById('error');

// Get the button that opens the modal
// var btn = document.getElementById("myBtn");

// Get the <span> element that closes the modal
var span = document.getElementsByClassName("close")[0];

// When the user clicks on <span> (x), close the modal
span.onclick = function() {
  modal.style.display = "none";
}

// When the user clicks anywhere outside of the modal, close it
window.onclick = function(event) {
  if (event.target == modal) {
    modal.style.display = "none";
  }
}
</script>